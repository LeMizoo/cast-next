specVersion: "v1"
serviceId: "cast-next"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cast-next-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
